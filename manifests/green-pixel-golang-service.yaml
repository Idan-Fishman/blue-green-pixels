apiVersion: v1
kind: Service
metadata:
  name: green-pixel-golang
  namespace: blue-green-pixels
spec:
  selector:
    app: green-pixel-golang
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
